import { reactive } from 'vue';
import { onLoad } from '@dcloudio/uni-app';

export const usePage = () => {
  const page = reactive({

  });

  onLoad((query) => {

  });

  return {
    page,
  };
};
